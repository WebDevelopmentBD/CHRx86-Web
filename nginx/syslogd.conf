stream {
    upstream python_backend {
        server 127.0.0.1:10514;
    }

    server {
        listen 514 udp;
        proxy_pass python_backend;
        proxy_responses 0;
        proxy_timeout 1s;
    }
}